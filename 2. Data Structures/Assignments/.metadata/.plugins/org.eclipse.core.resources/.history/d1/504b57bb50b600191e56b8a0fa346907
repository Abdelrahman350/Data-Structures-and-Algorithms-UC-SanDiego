/*
 * DetectLoop.c
 *
 *  Created on: Aug 2, 2019
 *      Author: Abdelrahman
 */


#include <stdio.h>
#include <stdlib.h>
#include "LinkedList.h"

int main(){
	list* L = create_list();
	push_back_list(L,50);
	push_back_list(L,20);
	push_back_list(L,15);
	push_back_list(L,4);
	push_back_list(L,10);
	while(!is_empty_list(L)){
		printf("%d ", pop_front_list(L));
		fflush(stdout);
	}
	return 0;
}
